# banco — SQLite em dev. Em producao: postgresql://... (trocar tambem o provider no schema.prisma)
DATABASE_URL=""

# assina os cookies de sessao
AUTH_SECRET=""

# criptografa a chave de IA do workspace em repouso (BYOK). Perder isso = perder as chaves salvas.
ENCRYPTION_KEY=""

# chave do servidor: fallback quando o workspace nao trouxe a propria (keySource=server)
ANTHROPIC_API_KEY=""
